# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本号采用语义化版本（SemVer）。
Git 标签与本文一一对应：`git tag` → `v1.0.0`、`v2.0.0`、`v3.0.0`、`v3.1.0`。

## [3.1.0] - 2026-09-27

按用户第三轮反馈（含**真实检测输出**）修正行高合规、主题色语义与工具栏位置。

### 修复
- **行高叠字告警（用户第 4 项，贴出 19 条实测告警）**：拿到官方判定源码 `cli/engine/layout.ts#detectLineHeightOverlap`——
  `lineHeight === 0`，或 **实测平均行高 `contentHeight/lineCount < fontSize × 0.95`** 即判「行高小于字体大小」。
  根因是文章里「大字号行内元素（强调/数字）继承了较小的 px 行高」以及承载 `body` 样式的根节点行高为 0。
  v3.1 在内联化时与转换后各做一次**行高合规归一**：含文字的元素若 `line-height` 为 0 或小于字号，一律抬到 `1.2×字号`；
  复制/下载前再兜底一次。状态栏会报告「修正 N 处过小行高」。自检新增两个真实场景夹具（`line-height:0`、大字号行内），均 0 违规。
- **主题色语义错误（用户第 3 项）**：v3.0 会把「品牌色族」整体替换，导致**整篇文字颜色一起变**，且界面按钮（`--theme`）也跟着变色。
  v3.1 改为**按角色施加**（对齐 nihaotk 的实际行为）：
  ① 小节标题（文字色 + 原有装饰线）② 卡片/引用块的浅色底纹与边框线条 ③ 段落中重要的第一句话（段首 `strong`）
  ④ 表格表头底色、分割线。**正文段落文字颜色不再改变**。
  浅色底的判定改用「与白底合成后的观感」（半透明浅底如 `rgba(10,94,68,.06)` 也能正确跟随），保留原透明度与明度。
  另：界面强调色拆分为固定的 `--ui`，文章主题色**不再影响**「复制」「转换并适配公众号」按钮与版本徽章。
- **主题色板文字重叠（用户第 3 项）**：色板按钮改为两列网格、最小宽度 96px、`white-space:nowrap` + 文本省略，
  名称与色块不再挤压；新增断言（按钮互不重叠、宽度 ≥96px、文字不被裁切）。
- **工具栏位置（用户第 1 项）**：工具栏从「顶栏整条」移到**预览区上方（右侧栏内）**；顶栏只留标题与「下载 / 复制」。
  左栏保持纯输入区（输入 HTML + 按钮 + 输出代码）。

### 新增
- **一键清除全部色块**（用户第 2 项）：「样式 → 色块 → 一键清除全部色块」，一次清掉全文色块底色与文字高亮的底色，
  **保留卡片（带边框）与整篇底色**，状态栏报告清除数量，可 Ctrl+Z 撤销。
- 手动「移除色块底色 / 移除卡片边框 / 移除卡片背景」时，同步清除该元素的主题角色记录，避免再次换色时把底色「复活」。
- 主题角色以「元素 + 原值」登记：插入新元素时增量登记；撤销/重做导致 DOM 重建后按当前状态重新登记。

### 变更
- 标题样式选项重排：**主题色文字（默认）** / 下边框 / 左边框 / 不施加 —— 默认即让小节标题跟随主题色。
- 测试：功能 40 项 + 布局 30 项 + 公众号合规 21 项 = **91 项断言**。

## [3.0.0] - 2026-09-27

按用户第二轮反馈（6 项）重构界面与主题色引擎；动手前完成 md.nihaotk.com 逐项实测（CDP 抓真实 DOM/几何/菜单）与微信官方校验器源码研读，结论见 [docs/PLAN-v3.0.0.md](./docs/PLAN-v3.0.0.md)。

### 修复
- **仍然出现「内容结构检测」警告（用户问题 4，真因找到）**：官方校验器 `rules/font-family.ts` 规定——内联 `font-family` **不以 `"mp-quote", PingFang SC, system-ui, -apple-system` 开头即判违规**。v2 虽做了 text-align/white-space 修正，却仍给**每个元素**写了 `font-family:-apple-system,"PingFang SC",…` → 全线命中。v3 从白名单彻底移除 `font-family`，转换时一律剥离（并移除字体族选择器）。
  另按 `rules/width.ts` 修正：`table > thead > th` 会被当作 width 候选，因此**自动归一为 `tbody/td`**（内联样式原样保留，视觉不变）。
  新增 `test-wechat-spec.html`：**移植官方 12 条规则**做本地自检（含 375px 实测溢出/叠字），19 项全绿。
- **主题色与 nihaotk 差距过大（用户问题 3）**：v2 用「按精确色值替换」，浅色底纹与深色描边不跟随、且换到红色主题后文章原有红色会被误纳入色族（漂移）。v3 改为**基线登记 + 从基线重导**：转换时记录每个元素/属性的颜色原值与所属「品牌色族」，换色时按 HSL 映射并**保留 alpha 与明度/饱和度差**；往返切换结果**逐字节一致**（已断言）。色板改为 nihaotk 实测的 11 色（带名称 + 色块，三列），并新增「还原」回到原始配色。
  另修：饱和度映射上限误设 0.98，导致 `#009874` 这类高饱和色被削成 `rgb(2,150,115)`；改回上限 1 后精确还原主题色。
- **卡片与色块混淆（用户问题 5）**：两者在**插入、编辑、识别、状态提示**四个层面彻底分离。
  · 色块 = 纯底色盒（无边框），卡片 = 带边框容器（`border` + `background`）；
  · 控件按目标分组：「样式 → 色块」与「样式 → 卡片」各自独立；
  · `findContainer()` 按「有可见边框 → 卡片 / 只有底色 → 色块 / 都不是 → 段落」识别，并在状态栏显示「目标：卡片 / 色块 / 段落」；
  · 普通段落不会再被误判为卡片而错改主体容器（v2 的 `findCard` 存在此风险，已断言修复）。
- **主体底色（用户问题 2）**：整项移除（含控件与逻辑）。

### 新增
- **Word 式菜单条置于顶栏最左**（用户问题 1）：文件 / 编辑 / 格式 / 插入 / 样式 / 帮助，点击展开、切换互斥、Esc 或点空白关闭；每个下拉面板内按 **Word 功能区分组**（组名在组底部 + 组分隔线），所有排版控件归类收纳，解决「工具栏乱」。
- **界面布局对齐 nihaotk**：顶栏 56px + 左输入 / 右预览双栏 + **可拖拽分隔条**（双击复位、宽度记忆）；预览支持**电脑端自适应 / 移动端 375px 手机框（描边 + 阴影）**切换。
- **标题样式**（对应 nihaotk 的「标题样式」）：默认 / 主题色文字 / 下边框 / 左边框，且施加后会跟随后续主题色切换。
- 快捷键：`Ctrl+↵` 转换、`Ctrl+Shift+C` 复制、`Ctrl+D` 删除线。
- 输出代码区改为可折叠；状态栏常驻显示操作反馈与「当前目标容器」。
- 新增测试：`test-wechat-spec.html`（19 项合规断言）；`test-harness.html` 扩到 45 项、`test-layout.html` 扩到 27 项。

### 变更
- 顶部标题/顶栏结构重排；「段落背景」更名「段落底色」；「卡片底色 / 卡片线条」合并为语义明确的「卡片组」。
- 版本文档：新增 `docs/PLAN-v3.0.0.md`（nihaotk 实测数据 + 微信规则源码结论 + 设计方案 + 验收清单）。

## [2.0.0] - 2026-09-27

按用户反馈的 8 项问题整体重构工具栏与导出管线。

### 新增
- **Word 式功能区工具栏**：QAT 快捷栏（复制/下载/撤销/重做/预览宽度/折叠）+ 分组功能区（字体 | 段落 | 插入 | 卡片 | 主题，组名置底）+ 固定高度状态栏，整体复刻 Word 与 [md.nihaotk.com](https://md.nihaotk.com/) 的布局习惯。
- **主题色一键全局替换**：转换后自动识别全文品牌色（标题/加粗/表头/引用块加权投票，中性色排除）；11 个预设色（经典蓝/翡翠绿/活力橘/柠檬黄/薰衣紫/天空蓝/玫瑰金/橄榄绿/石墨黑/雾烟灰/樱花粉）+ 自定义取色；替换覆盖同 RGB 不同透明度的浅色底；可重新识别、可撤销。
- **段落边框**：无 / 左侧强调线 / 全边框 + 边框色，作用于选中的段落本身。
- **卡片边框**：颜色 + 宽度（2/3/4px）+ 「无」，作用于卡片容器本身。
- **首行缩进**：段落组内 2em 开关。
- **预览宽度切换**：375px（移动端模拟）/ 578px（默认），复刻 nihaotk 的「移动端/电脑端」切换。
- **移除全部字体**：一键清空全文 `font-family`，跟随公众号默认字体栈（规范第 3 条建议）。
- **导出前扁平化**（`normalizeExport`）：合并同样式单子节点 span 链、解包冗余 span、删除空 span，规避规范 2.1「嵌套层级限制」触发的结构检测告警；复制与下载时自动执行。
- **内联输出降噪**：裁掉可证明为 no-op 的声明 —— 0 宽/无边框的 `border*`（含被各边简写覆盖的 `border-width/style/color` 长写）、无背景图时的 `background-repeat/position/size`、全 0 的 `margin/padding`、非列表元素的 `list-style*`、默认值 `vertical-align:baseline` / `word-spacing:0` / `text-indent:0` / `border-radius:0px`，以及被 4 值简写完全覆盖的 `padding/margin/圆角` 长写。样例文章输出由 **32.3 KB 降至 8.9 KB（−72%）**，关键样式全部保真（有断言）。
- **版本徽章**：页面标题旁显示当前版本号。

### 修复
- **工具栏载入后莫名增大**（用户问题 2，根因两处）：
  1. **文章 CSS 泄漏污染工具界面（主因）**：v1 把原文 `<style>` 注入到工具页面内的 `#sandbox` div 中，文章样式（如样例的 `body{padding:32px 16px}`、`*{margin:0;padding:0}`、`body{background}`）**同时作用于本工具自身**，载入不同文章后工具栏被重新排版 —— 实测工具页 body 内容宽度由 1185px 被压到 1153px、页头同步变窄、group 行高 ±2px，功能区换行随之改变。v2 改用**隐藏 iframe 做样式隔离沙箱**（`about:blank` + 同源 `contentDocument`），文章 CSS 只作用于 iframe 内部，`getComputedStyle` 也改为在该 iframe 的 window 上计算，隔离后载入前后所有测量值完全一致。
  2. **选区信息条撑高工具栏**：v1 的 `#selinfo` 在载入并点击预览后 `display:block` 撑高工具栏、字体下拉动态插入长文本选项加宽控件。v2 改为**固定高度 24px 状态栏**（左操作状态 / 右选区信息，单行省略）+ 全部下拉框**固定宽度**。
  另修：`.layout` 栅格轨道改 `minmax(0,…)` 并加 `width:100%`、`html{overflow-y:scroll}` 常驻滚动条、`.grp-name`/`.mini` 显式 `line-height`，消除内容/滚动条引起的宽度与行高抖动。
  回归测试：`test-layout.html`（11 项断言，含 body padding/背景色泄漏检测）。
- **段落背景位置**（用户问题 1）：从 v1 第二行混在「整篇/主体/卡片底色」之间，移入**段落分组**，与对齐/行距/段距/段落边框同组。
- **公众号「内容结构检测」告警**（用户问题 4）：根因是 v1 `inlineTree` 用 `getComputedStyle` 拷贝 `text-align` 时把默认计算值 `start` 原样内联（规范 1.6 明确禁止 start/end）。v2 在内联化时丢弃 `start`/`end`；同时把 `white-space:pre` 改写为 `pre-wrap`（规范 1.8）；剥离 0 宽边框噪音属性。
- **色块无法删除**（用户问题 5 前半）：段落背景 / 卡片背景 / 卡片边框 / 文字背景全部补上「无」按钮，实现为**移除内联属性**而非写 `transparent`，输出更干净。
- **卡片边框线条定位不对**（用户问题 3 / 5 后半）：v1 `findCard()` 向上遍历时会把普通段落上方的主体 `DIV` 误判为卡片，导致边框/背景画错容器。v2 重写：`BLOCKQUOTE`/`ASIDE` 直接命中；`SECTION`/`DIV` 需自带可见背景色或非零左边框才算卡片；显式跳过根 section 与全部主体容器。

### 变更
- 顶栏文案与提示更新，标注 v2 合规行为。
- 「获取可用模型」式的动态宽度下拉全部固定宽度。

## [1.0.0] - 2026-09-26

初始版本（原始文件存档：`versions/v1.0.0.html`）。

### 功能
- HTML → 公众号内联样式转换：DOMParser 解析、578px 屏幕外沙箱 `getComputedStyle` 计算、CSS 变量展开、`@media` 走通、白名单拍平内联。
- 可视化编辑：吸顶工具条（字体/字号/加粗斜体下划线/对齐/字色/文字背景/行距/段距/段落背景/整篇底色/主体底色/卡片底色）、分割线与引用卡片插入、撤销/重做（80 步快照栈）、选区信息回显。
- 复制（带格式，Clipboard API + execCommand 兜底）、下载 .html。
- 载入样例 / 选择本地 .html 文件。
